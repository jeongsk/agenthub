---
import { Terminal, Plus } from '@lucide/astro';
import GitHub from './icons/GitHub.astro';
---

<header class="main-header glass-panel">
  <div class="container header-container">
    <a href="/" class="logo" id="header-logo-link">
      <div class="logo-icon">
        <Terminal size={20} class="cyan-icon" />
      </div>
      <span class="logo-text">Agent<span class="text-gradient">Hub</span></span>
    </a>
    
    <nav class="nav-links">
      <a href="/" class="nav-link" id="nav-home">Browse</a>
      <a href="/submit" class="nav-link" id="nav-submit-guide">Contribute</a>
      <a href="https://github.com" target="_blank" rel="noopener noreferrer" class="nav-icon-link" aria-label="GitHub Repository">
        <GitHub size={20} />
      </a>
    </nav>
    
    <div class="header-action">
      <a href="/submit" class="btn-grad text-sm" id="header-submit-btn">
        <Plus size={16} />
        <span>Submit Skill</span>
      </a>
    </div>
  </div>
</header>

<style>
  .main-header {
    position: fixed;
    top: 16px;
    left: 16px;
    right: 16px;
    height: 70px;
    z-index: 100;
    border-radius: 20px !important;
    display: flex;
    align-items: center;
  }

  .header-container {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 24px;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: white;
    font-size: 1.35rem;
    font-weight: 800;
    font-family: 'Outfit', sans-serif;
  }

  .logo-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: rgba(6, 182, 212, 0.1);
    border: 1px solid rgba(6, 182, 212, 0.25);
    border-radius: 10px;
    transition: all 0.3s ease;
  }

  .logo:hover .logo-icon {
    transform: rotate(10deg) scale(1.05);
    background: rgba(6, 182, 212, 0.15);
    border-color: rgba(6, 182, 212, 0.4);
    box-shadow: 0 0 10px rgba(6, 182, 212, 0.2);
  }

  .cyan-icon {
    color: hsl(var(--cyan-point));
  }

  .logo-text {
    letter-spacing: -0.03em;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 30px;
  }

  .nav-link {
    color: hsl(var(--text-secondary));
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: hsl(var(--text-primary));
  }

  .nav-icon-link {
    color: hsl(var(--text-secondary));
    transition: color 0.2s ease, transform 0.2s ease;
    display: flex;
    align-items: center;
  }

  .nav-icon-link:hover {
    color: white;
    transform: scale(1.05);
  }

  .header-action {
    display: flex;
    align-items: center;
  }

  .text-sm {
    padding: 8px 16px;
    font-size: 0.85rem;
    border-radius: 10px;
  }

  /* Responsive styles */
  @media (max-width: 640px) {
    .nav-links {
      gap: 16px;
    }
    .header-action {
      display: none; /* hide CTA button on mobile */
    }
  }
</style>
