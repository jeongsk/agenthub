interface:
  display_name: "Analog Reading Note Image"
  short_description: "손글씨 감성 독서기록 이미지 생성"
  default_prompt: "Use $analog-reading-note-image to ask for any missing book details, then generate a Korean handwritten reading journal image directly."
policy:
  allow_implicit_invocation: true
